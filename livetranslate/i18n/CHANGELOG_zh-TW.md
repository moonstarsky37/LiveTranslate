# 更新日誌

## 2026-08-07 (v2026.08.07-tw.1, 本 fork 首次發版)
- 本版起為 moonstarsky37/LiveTranslate fork: 模型下載全面改走 HuggingFace (曾以 ModelScope 下載的舊快取仍可讀), 台灣連線不再逾時
- 首啟精靈重設計: 移除 15 秒自動倒數, 下載只在明確按下按鈕後開始; 設定於按下當下即寫入, 下載中斷後重啟直接續走缺模型下載流程, 不再重回精靈迴圈
- 繁體中文 (zh-TW) 升為第一級語言: 新增繁中介面與繁中主文件, zh-TW/zh-HK 系統預設繁中; 舊設定 "zh" 自動遷移為 zh-TW
- 修復 VAD: 短於最短語音長度的尾句在持續靜音後改為強制送出, 暫停前的最後一句不再消失
- 修復 requirements.txt 漏列 pysbd 導致全新安裝在增量辨識時崩潰的問題
- 內部重構 (Phase 1-3): 程式碼 package 化 (livetranslate/), 設定統一入口 SettingsStore, main / control_panel / overlay / supervisor 巨石檔全數拆分, 行為以 AST 比對與特性測試釘住
- 建立 CI 品質關卡 (ruff + mypy + pytest) 與 branch protection: CI 不綠進不了 main

## 2026-07-11
- 修復 Fun-ASR-Nano 首次載入時 Qwen3-0.6B 權重下載被 180 秒逾時中斷的問題 (#32): 權重改為在模型下載階段提前取得, ASR worker 啟動不再等待大檔案下載
- 修復 ModelScope 1.38+ 快取位置變更導致模型漏偵測的問題 (#32, #33): 相容各版本 SDK 的全部快取目錄配置, 升級 SDK 後不再重複下載已有的模型
- Qwen3-0.6B 權重支援從 ModelScope 下載, 使用 ModelScope 來源的使用者不再被迫走 HuggingFace (#33)

## 2026-06-20
- 新增「遠端 Whisper」ASR 引擎: 把語音辨識外包到有 GPU 的另一台機器 (附 `asr_server.py` 伺服器端), 沒有 GPU 的機器也能即時辨識
- 新增「WebID 身分核驗」翻譯提示詞預設集: 針對影片核驗情境調校用詞
- ASR 改為子行程隔離執行: worker 當掉/逾時自動重啟, 記憶體成長到門檻值自動回收, 辨識故障不再拖垮介面
- 字幕視窗支援滑鼠穿透 (#28): 字幕設定裡新增開關, 系統匣選單可用「字幕滑鼠穿透」快速切換, 開啟後點擊會直達後方視窗 (此時中鍵拖曳失效, 先關穿透再移動)

## 2026-05-10
- 新增「匯出到檔案」: 支援原文 / 譯文 / 原文+譯文 三種格式, 字幕浮窗右鍵選單與系統匣選單均可觸發
- 新增「逐字稿存檔」(預設開啟): 每次工作階段自動在 `transcripts/` 下建立 3 份檔案 (原文 / 譯文 / 全部), 每段辨識結果即時追加寫入, 不再因字幕浮窗 50 則上限而遺失早期文字
- 設定面板「快取」標籤新增「逐字稿存檔」分組: 開關 + 開啟逐字稿資料夾按鈕
- 新增記憶體佔用上限保護: RSS 超過 4096MB 時跳出系統匣通知提示重新啟動 (ASR 後端在 native 側有工作區/快取, Python GC 與 `torch.cuda.empty_cache` 不一定能回收)
- 日誌新增 `MEM[asr#/tick]` 行: 每次真實 ASR 呼叫後輸出 RSS / GPU(alloc/reserved) / 音訊時長 / overlay 訊息數 / VAD buffer 長度, 便於排查記憶體問題

## 2026-04-20
- 移除 Qwen3-ASR 引擎 (ONNX + GGUF 方案相容性較差, 相關模型檔案和 llama.cpp 執行時依賴一併清理)
- 模型設定新增「進階參數」: `temperature` / `top_p` / `max_tokens` / `frequency_penalty` / `presence_penalty` / `seed`, 每項有獨立的「覆寫」開關, 未勾選時使用伺服器端預設值
- 模型設定新增 `extra_body` (JSON): 供應商專屬參數透傳, 如 `thinking_budget`、`reasoning_effort` 等, 儲存時自動驗證 JSON 格式
- 修復 Anime-Whisper 模型未快取時下載對話框靜默無反應的問題
- 修復設定面板「更新日誌」Tab 空白的問題 (正規表示式比對 H3 但檔案用 H2, 從 3 月加入功能起就失效)

## 2026-04-18
- 新增 ASR 引擎: Anime-Whisper (litagin/anime-whisper), 日語動畫/Galgame 特化, 擅長辨識喘息/嘆息等非語言發聲
- 修復 HF 快取偵測誤判: 下載中斷留下的空目錄不再被認為「已快取」

## 2026-03-31
- 管線執行緒拆分: 原本 capture+VAD+ASR 單執行緒改為 capture 與 ASR 兩條獨立執行緒, 長句 ASR 不再阻塞 RMS/VAD 條的即時更新
- ASR 排程新增有界佇列 (16 段), 佇列滿時丟棄最舊的 interim 段, 避免堆積造成延遲

## 2026-03-26
- 預設翻譯提示詞最佳化: 新增 ASR 校正規則 (辨識出錯字/同音字時盡量依上下文修正)、流暢性規則 (避免逐字直譯)

## 2026-03-25
- 樣式標籤新增「重設視窗位置」按鈕: 字幕視窗回到 (100,100), 主字幕浮窗回到螢幕右下角
- 字幕視窗預設位置從螢幕底部置中改為 (100,100), 最小高度調整為 200px
- 視窗位置還原時會驗證是否在可見螢幕範圍內 (使用 availableGeometry 排除工作列), 高度變化後 clamp 到螢幕邊界

## 2026-03-24
- 字幕視窗: 超長文字自動換行顯示(不再分段), 背景高度平滑動畫, 文字渲染 Pixmap 快取
- 主字幕浮窗/字幕視窗: 位置和大小自動記憶, 重新啟動後還原
- 主字幕浮窗: 精簡模式切換動畫
- 設定: 移除設定檔 key 白名單限制

## 2026-03-23
- 品牌更名 LiveTrans → LiveTranslate
- 模型設定新增: 串流傳輸、結構化輸出、上下文數、停用思考(預設開啟)
- 翻譯結果串流逐字顯示
- 提示詞最佳化: 禁止多候選翻譯, 編輯即時生效
- 模型重複輸出偵測與提示
- ASR 引擎標註[準確]/[快速]
- 設定面板新增更新日誌 Tab
